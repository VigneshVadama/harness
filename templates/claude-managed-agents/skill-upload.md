# Upload Skill

Upload `skills/orchestrator-worker-validator/` as a custom Claude Agent Skill first.

Record the returned skill id:

```sh
export HARNESS_SKILL_ID=skill_...
```

The templates in this directory use `$HARNESS_SKILL_ID` with `version: latest`.
